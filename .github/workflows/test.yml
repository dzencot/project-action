name: "test"
on:
  pull_request:
  push:
    branches:
      - master
      # - 'releases/*'

jobs:
  jest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: make install
    - run: make test

  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ./
      env:
        NODE_DEBUG: http
        HTTPS_PROXY: localhost
      with:
        project-member-id: 1
        verbose: true
